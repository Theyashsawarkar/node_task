info:
  name: Upload file
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/file"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/yash/Pictures/lockscreen/background.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
