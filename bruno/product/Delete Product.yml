info:
  name: Delete Product
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/product/:productId"
  params:
    - name: productId
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
