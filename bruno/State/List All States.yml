info:
  name: List All States
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/country"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
