info:
  name: Add State
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/state"
  body:
    type: json
    data: |-
      {
        "name": "maharashtra",
        "countryId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
