info:
  name: List Sellers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/seller?search=&page=&limit="
  params:
    - name: search
      value: ""
      type: query
    - name: page
      value: ""
      type: query
    - name: limit
      value: ""
      type: query

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This request retrieves a list of users from the JSONPlaceholder API.
