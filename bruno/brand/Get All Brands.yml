info:
  name: Get All Brands
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/brand"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
