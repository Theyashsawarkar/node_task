info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/country"
  body:
    type: json
    data: |-
      {
        "name": "india"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
